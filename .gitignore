# Xcode
*.xcuserstate
xcuserdata/
*.xccheckout
*.moved-aside
*.xcscmblueprint
DerivedData/
.build/

# macOS
.DS_Store

# Swift Package Manager
.swiftpm/
Package.resolved

# CocoaPods (nếu dùng sau này)
Pods/

# Firebase config (nhạy cảm, không commit)
GoogleService-Info.plist

# Config môi trường — .env.example thì commit, .env thì không
.env
Config.xcconfig

# SDK eKYC (CmcEkycSDK + KalapaSDK + iProov) — ~31MB nhị phân, không đưa vào git để
# repo khỏi phình mỗi lần SDK lên phiên bản. Xem Frameworks/README.md để lấy về.
Frameworks/*
!Frameworks/README.md

PLAN.md
